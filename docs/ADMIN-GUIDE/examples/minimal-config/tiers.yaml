# =============================================================================
# MINIMAL CONFIG: tiers.yaml
# =============================================================================
# This is a minimal 2-tier configuration.
# Most institutions use 3-4 tiers. Expand as needed.
# =============================================================================

tiers:
  # -------------------------------------------------------------------------
  # TIER 1: Standard / Non-sensitive
  # -------------------------------------------------------------------------
  - slug: standard
    name: "Standard"
    short_name: "Standard"
    sort_order: 1
    color: "green"                # UI color: green, yellow, orange, red
    description: "Non-sensitive research data"

    # Examples help users understand what belongs here
    examples:
      - "Published datasets"
      - "Public domain images"
      - "Pre-publication manuscripts"
      - "Non-identifiable survey data"

    # Workflow settings
    consultation_required: false        # No consultation needed
    retention_questions_required: false # Skip the retention questions step

  # -------------------------------------------------------------------------
  # TIER 2: Sensitive / Regulated
  # -------------------------------------------------------------------------
  - slug: sensitive
    name: "Sensitive"
    short_name: "Sensitive"
    sort_order: 2
    color: "orange"
    description: "PHI, PII, or other regulated data requiring compliance controls"

    examples:
      - "Patient medical records (HIPAA)"
      - "Student education records (FERPA)"
      - "Identifiable human subjects data"

    # Workflow settings
    consultation_required: true        # Show the consultation step before service selection
    retention_questions_required: true # Ask retention questions for regulated data
    consultation_message: |
      Projects with sensitive data require a consultation to verify
      compliance requirements before provisioning services.
    consultation_contact: "rc-help@example.edu"
